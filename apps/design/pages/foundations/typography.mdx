---
title: Typography
---

import { FoundationsTypography, FoundationsGeist } from "@components/foundations/typography.mdx";

# 🖋️ Typography

Typography is the suite of fonts we use to guide our users with the right tone through every step of their experience.

## Font

Our design system utilizes the [Geist font](https://vercel.com/font/sans), an open-source variable font that offers adjustable knobs for fine-tuning font weight and slant.

For languages where Geist doesn’t offer a complete set of glyphs, employs the system font to ensure proper rendering and compatibility.

```css copy
font-family: “GeistSans”, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue",
  Arial, "Noto Sans", sans-serif, "Apple Color Emoji", "Segoe UI Emoji", "Segoe UI Symbol", "Noto Color Emoji";
```

<FoundationsGeist />

## Font Mono

In our products, we primarily utilize sans serif typefaces for typography, reserving monospace fonts exclusively for instances where code needs to be displayed.

```css copy
font-family: "GeistMono", ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, "Liberation Mono",
  "Courier New", monospace;
```

## Font Stack

### Sans serif

<FoundationsTypography fonts={fontsSansSerif} />

### Monospaced

<FoundationsTypography fonts={fontsMono} />

export const fontsSansSerif = [
  { os: "All", fontFamily: "GeistSans" },
  { os: "All", fontFamily: "Helvetica Neue (fallback)" },
  { os: "All", fontFamily: "Arial (fallback)" },
  { os: "All", fontFamily: "sans-serif (fallback)" },
  { os: "All", fontFamily: "Noto Color Emoji (fallback)" },
  { os: "macOS & iOS", fontFamily: "-apple-system (fallback)" },
  { os: "macOS & iOS", fontFamily: "BlinkMacSystemFont (fallback)" },
  { os: "macOS & iOS", fontFamily: "Apple Color Emoji" },
  { os: "Windows", fontFamily: "Segoe UI (fallback)" },
  { os: "Windows", fontFamily: "Segoe UI Emoji" },
  { os: "Windows", fontFamily: "Segoe UI Symbol (fallback)" },
  { os: "Android & Chrome OS", fontFamily: "Roboto (fallback)" },
  { os: "Android & Chrome OS", fontFamily: "Noto Sans (fallback)" },
];

export const fontsMono = [
  { os: "All", fontFamily: "GeistMono" },
  { os: "All", fontFamily: "monospace (fallback)" },
  { os: "macOS & iOS", fontFamily: "SFMono-Regular (fallback)" },
  { os: "macOS & iOS", fontFamily: "Menlo (fallback)" },
  { os: "macOS & iOS", fontFamily: "Monaco (fallback)" },
  { os: "Windows", fontFamily: "Consolas (fallback)" },
  { os: "Windows", fontFamily: "Courier New (fallback)" },
  { os: "Linux", fontFamily: "Liberation Mono (fallback)" },
];
