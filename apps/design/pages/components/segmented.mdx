---
title: Segmented
---

import { Tabs, Cards, Card } from "nextra/components";
import { AccessibilityTable } from "@components/accessibility/accessibility-table.mdx";
import { segmentedAccessibilityData } from "@components/accessibility/segmented.tsx";
import {
  SegmentedPreview,
  SegmentedDefaultValue,
  SegmentedDisabled,
  SegmentedWithIcon,
} from "@components/preview/segmented.tsx";

# 💠 Segmented

Used to organize content by grouping similar information on the same area.

<Tabs items={['Preview', 'Code']}>
  <Tabs.Tab>
    <SegmentedPreview />
  </Tabs.Tab>
  <Tabs.Tab>
    ```js copy filename="segmented.tsx"
    import { Segmented, SegmentedContent, SegmentedList, SegmentedTrigger } from "@fusillo/ui/components/segmented"

    export function SegmentedDemo() {
      return (
        <Segmented defaultValue="grid">
          <SegmentedList>
            <SegmentedTrigger value="grid">Grid</SegmentedTrigger>
            <SegmentedTrigger value="list">List</SegmentedTrigger>
          </SegmentedList>
          <SegmentedContent value="grid">Grid view</SegmentedContent>
          <SegmentedContent value="list">List view</SegmentedContent>
        </Segmented>
      )
    }
    ```

  </Tabs.Tab>
</Tabs>

## Usage

The Segmented Control component allows users to choose between two or more mutually exclusive options. It is displayed as a horizontal set of segments, each representing an option, with the current selection highlighted.

This component is often used to switch between different views, filters, or modes within the same context.

### When to use

- **Mode switching**: Use Segmented Controls for changing modes within a particular context, such as switching between list and grid views in a gallery.
- **Filtering content**: Employ Segmented Controls to filter content on a page, such as sorting items by price or date.

Avoid using Segmented Controls when the options represent a workflow or a process with sequential steps; in such cases, a stepper or progress indicator might be more appropriate.

## Examples

### Default

<Tabs items={['Preview', 'Code']}>
  <Tabs.Tab>
    <SegmentedPreview />
  </Tabs.Tab>
  <Tabs.Tab>
    ```js copy filename="segmented.tsx"
    import { Segmented, SegmentedContent, SegmentedList, SegmentedTrigger } from "@fusillo/ui/components/segmented";

    export function SegmentedDemo() {
      return (
        <Segmented defaultValue="grid">
          <SegmentedList>
            <SegmentedTrigger value="grid">Grid</SegmentedTrigger>
            <SegmentedTrigger value="list">List</SegmentedTrigger>
          </SegmentedList>
          <SegmentedContent value="grid">Grid view</SegmentedContent>
          <SegmentedContent value="list">List view</SegmentedContent>
        </Segmented>
      )
    }
    ```

  </Tabs.Tab>
</Tabs>

### Default value

<Tabs items={['Preview', 'Code']}>
  <Tabs.Tab>
    <SegmentedDefaultValue />
  </Tabs.Tab>
  <Tabs.Tab>
    ```js copy {5} /defaultValue/ filename="segmented.tsx"
    import { Segmented, SegmentedContent, SegmentedList, SegmentedTrigger } from "@fusillo/ui/components/segmented";

    export function SegmentedDemo() {
      return (
        <Segmented defaultValue="list">
          <SegmentedList>
            <SegmentedTrigger value="grid">Grid</SegmentedTrigger>
            <SegmentedTrigger value="list">List</SegmentedTrigger>
          </SegmentedList>
          <SegmentedContent value="grid">Grid view</SegmentedContent>
          <SegmentedContent value="list">List view</SegmentedContent>
        </Segmented>
      )
    }
    ```

  </Tabs.Tab>
</Tabs>

### Disabled

<Tabs items={['Preview', 'Code']}>
  <Tabs.Tab>
    <SegmentedDisabled />
  </Tabs.Tab>
  <Tabs.Tab>
    ```js copy {8} /disabled/ filename="segmented.tsx"
    import { Segmented, SegmentedContent, SegmentedList, SegmentedTrigger } from "@fusillo/ui/components/segmented";

    export function SegmentedDemo() {
      return (
        <Segmented defaultValue="grid">
          <SegmentedList>
            <SegmentedTrigger value="grid">Grid</SegmentedTrigger>
            <SegmentedTrigger value="list" disabled>List</SegmentedTrigger>
          </SegmentedList>
          <SegmentedContent value="grid">Grid view</SegmentedContent>
          <SegmentedContent value="list">List view</SegmentedContent>
        </Segmented>
      )
    }
    ```

  </Tabs.Tab>
</Tabs>

### With icon

<Tabs items={['Preview', 'Code']}>
  <Tabs.Tab>
    <SegmentedWithIcon />
  </Tabs.Tab>
  <Tabs.Tab>
    ```js copy {8,9} filename="segmented.tsx"
    import { Segmented, SegmentedContent, SegmentedList, SegmentedTrigger } from "@fusillo/ui/components/segmented";
    import { Grid, List } from 'lucide-react';

    export function SegmentedDemo() {
      return (
        <Segmented defaultValue="grid">
          <SegmentedList>
            <SegmentedTrigger value="grid"><Grid /></SegmentedTrigger>
            <SegmentedTrigger value="list"><List /></SegmentedTrigger>
          </SegmentedList>
          <SegmentedContent value="grid">Grid view</SegmentedContent>
          <SegmentedContent value="list">List view</SegmentedContent>
        </Segmented>
      )
    }
    ```

  </Tabs.Tab>
</Tabs>

## Useful links

**Here you go!** Here are all the resources you might need if you are a designer, a developer or a content manager:

<Cards className="mt-8">
  <Card
    title="🤖 Storybook"
    href="https://www.stories.fusillo.design/?path=/docs/components-segmented--documentation"
  />
  <Card
    title="🪄 Figma"
    href="https://www.figma.com/design/Jfto7FUoU7mSpnv9uESD60/%F0%9F%9F%A0-fusillo---design-system?node-id=763-3968&t=nua4UmG9Iu1hfUAm-1"
  />
  <Card
    title="🐝 API Reference"
    href="https://www.radix-ui.com/primitives/docs/components/tabs#api-reference"
  />
</Cards>

## Accessibility

Adheres to the [Tabs WAI-ARIA design pattern](https://www.w3.org/WAI/ARIA/apg/patterns/tabs/).

### Keyboard interactions

<AccessibilityTable rows={segmentedAccessibilityData} />
