import border from "@fusillo/config/tokens/tokens";

export const TokensBorder = ({ type }) => (
  <>
    {
      <table className="docs my-6">
        <thead>
          <tr>
            <th align="left">Token and description</th>
            <th align="right">
              <span className="pr-4">Value</span>
            </th>
            <th className="hidden sm:table-cell" align="left">
              Preview
            </th>
          </tr>
        </thead>
        <tbody>
          {Object.entries(border.tokens.light.border[type]).map(([tokenName, tokenInfo]) => (
            <tr key={tokenName}>
              <td className="w-full">
                <div className="cell-tokens-name">
                  <code className="font-medium">
                    border-{type}-{tokenName}
                  </code>
                  <div className="cell-description-tokens">{tokenInfo.description}</div>
                </div>
              </td>
              <td>
                <div className="cell-value-scale" align="right">
                  {tokenInfo.value}
                </div>
              </td>
              <td className="hidden sm:table-cell">
                <div
                  className={
                    type === "radius"
                      ? "cell-preview-border-radius"
                      : type === "width"
                      ? "cell-preview-border-width"
                      : {}
                  }
                  style={
                    type === "radius"
                      ? { borderRadius: tokenInfo.value }
                      : type === "width"
                      ? { borderWidth: tokenInfo.value }
                      : {}
                  }></div>
              </td>
            </tr>
          ))}
        </tbody>
      </table>
    }
  </>
);
